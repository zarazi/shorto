<div class="jumbotron text-center">
  <h1 class="display-3"><a href="{{ shortUrl }}"" target=" new">{{ shortUrl }}</a></h1>
  <p class="lead">{{ originalUrl }}</p>
</div>

<div style="text-align:center">
  <a class="btn btn-outline-primary" href="/">Another One</a>
</div>